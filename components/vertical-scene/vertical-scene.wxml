<view class="section-title">垂直领域</view>
<view class="scene-grid">
  <block wx:for="{{sceneList}}" wx:key="index">
    <view class="scene-card {{index < 12 ? 'card-gradient-1' : ''}}" 
          bindtap="handleCardClick" 
          data-scene="{{item.title}}" 
          data-index="{{index}}">
      <view class="card-content">
        <view class="card-left">
          <view class="card-text-wrapper">
            <text class="card-text">{{item.title}}</text>
            <text class="card-desc">{{item.desc}}</text>
          </view>
        </view>
        <image 
          class="card-image" 
          src="https://mini.9000aigc.com/assets/cards/cards-{{cardImages[index]}}.png" 
          mode="aspectFit"
        ></image>
      </view>
      <!-- <image wx:if="{{index >= 3 && index < 9}}" src="https://mini.9000aigc.com/assets/images/scene{{index + 1}}.png" mode="aspectFill"></image> -->
    </view>
  </block>
</view>