<view class="paid-course">
  <!-- 标题区域 -->
  <view class="section-title">
    <view class="title-bar"></view>
    <text>精选付费课程</text>
  </view>

  <!-- 课程卡片网格 -->
  <view class="course-grid">
    <view class="course-card" 
          wx:for="{{courses}}" 
          wx:key="id"
          bindtap="navigateToLesson"
          data-id="{{item.id}}"
          data-title="{{item.title}}"
          data-cover="{{item.coverImage}}"
          data-desc="{{item.description}}"
          data-teacher="{{item.teacherInfo}}">
      <image class="course-image" src="{{item.coverImage}}" mode="aspectFill"></image>
      <!-- 课程信息区域 -->
      <view class="course-info">
        <view class="course-title">{{item.title}}</view>
        <view class="course-meta">
          <view class="course-price">
            <!-- 显示会员价格或普通价格 -->
            <text class="price-tag">¥</text>
            <text class="price-value">{{item.displayPrice}}</text>
            <!-- 如果是会员且memberPrice为0，显示会员免费标签 -->
            <text class="member-free" wx:if="{{userType === 1 && item.memberPrice === 0}}">
              会员免费
            </text>
          </view>
          <view class="course-teacher">讲师：{{item.teacherInfo || '未知'}}</view>
        </view>
      </view>
    </view>
  </view>

  <!-- 加载状态 -->
  <view class="loading-status">
    <view wx:if="{{loading}}" class="loading">加载中...</view>
    <view wx:elif="{{!hasMore}}" class="no-more">没有更多了</view>
  </view>
</view> 